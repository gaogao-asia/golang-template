syntax = "proto3";

option go_package = "/pb";

message Memory {
    enum Unit {
        UNKNOW = 0;
        BIT = 1;
        BYTES = 2;
        KYLOBYTES = 3;
        MEGABYTES = 4;
        GIGABYTES = 5;
    }

    uint64 value = 1;
    Unit unit = 2;
}